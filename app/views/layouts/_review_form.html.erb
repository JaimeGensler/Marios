<%= render "layouts/errors", :object => @review %>

<%= form_for [@product, @review] do |f| %>
    <div>
        <%= f.label :author, 'Author: ' %>
        <%= f.text_field :author %>
    </div>
    <div>
        <%= f.label :content_body, 'Comments: ' %>
        <%= f.text_area :content_body %>
    </div>
    <div>
        <%= f.label :rating, 'Rating: ' %>
        <%= f.number_field :rating, min: 1, max: 5 %>
    </div>
    <%= f.submit %>
<% end %>
